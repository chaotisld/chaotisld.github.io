document.addEventListener("DOMContentLoaded", () =>{
    const params = new  URLSearchParams(window.localtion.search);
    const categoria = paramss.get('cat');

    if(!categoria) return;

    const datos = {
        "subreg":{
            
        }
    }

})